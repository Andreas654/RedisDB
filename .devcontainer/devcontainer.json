{
  "name": "Redis Codespace",
  "dockerComposeFile": "docker-compose.yml",
  "service": "dev",
  "workspaceFolder": "/workspaces/RedisDB",
  "shutdownAction": "stopCompose",
  "postCreateCommand": "echo 'Redis ist verfügbar. Verwende: redis-cli -h redis' || true",
  "postStartCommand": "redis-server --daemonize yes && nohup socat TCP-LISTEN:6379,fork,reuseaddr TCP:redis:6379 > /tmp/socat.log 2>&1 &",
  "forwardPorts": [6379],
  "customizations": {
    "vscode": {
      "extensions": [
        "cweijan.vscode-redis-client"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  }
}
